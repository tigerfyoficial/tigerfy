<div class="login-wrapper">
  <div class="login-card">
    <h1>⚡ TigerFy</h1>
    <p class="subtitle">Crie sua conta para acessar a plataforma</p>

    <% if (typeof message !== "undefined" && message) { %>
      <p style="color:#ff6b6b;font-size:13px;margin-bottom:10px;"><%= message %></p>
    <% } %>

    <form method="POST" action="/register" class="login-form">
      <label for="username">Usuário</label>
      <input type="text" id="username" name="username" placeholder="Digite seu usuário" required />

      <label for="email">E-mail</label>
      <input type="email" id="email" name="email" placeholder="Digite seu e-mail" required />

      <label for="password">Senha</label>
      <input type="password" id="password" name="password" placeholder="Digite sua senha" required />

      <button type="submit" class="btn-login">Criar Conta</button>
    </form>

    <div class="divider"></div>

    <p class="register-text">
      Já possui uma conta?
      <a href="/login">Entrar agora</a>
    </p>
  </div>
</div>

<style>
  /* (SEU CSS ORIGINAL – nada modificado) */
  body {
    font-family: "Inter", sans-serif;
    background: radial-gradient(circle at center, #0d0d0d 0%, #000 100%);
    color: #fff;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .login-wrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
  }

  .login-card {
    background: #111;
    border-radius: 16px;
    padding: 40px 50px;
    width: 360px;
    box-shadow: 0 0 40px rgba(255, 204, 0, 0.08);
    text-align: center;
  }

  .login-card h1 {
    color: #ffcc00;
    font-weight: 800;
    font-size: 26px;
    margin-bottom: 8px;
  }

  .subtitle {
    color: #aaa;
    font-size: 14px;
    margin-bottom: 25px;
  }

  .login-form {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .login-form label {
    text-align: left;
    font-size: 13px;
    font-weight: 600;
    color: #ccc;
  }

  .login-form input {
    background: #1a1a1a;
    border: 1px solid #222;
    border-radius: 8px;
    padding: 12px;
    color: #fff;
    transition: border-color 0.2s;
  }

  .login-form input:focus {
    border-color: #ffcc00;
    outline: none;
  }

  .btn-login {
    background: #ffcc00;
    color: #000;
    font-weight: 700;
    border: none;
    border-radius: 8px;
    padding: 12px;
    cursor: pointer;
    transition: background 0.3s ease;
  }

  .btn-login:hover {
    background: #ffd633;
  }

  .divider {
    height: 1px;
    background: #222;
    margin: 30px 0;
  }

  .register-text {
    font-size: 13px;
    color: #aaa;
  }

  .register-text a {
    color: #ffcc00;
    text-decoration: none;
    font-weight: 600;
  }

  .register-text a:hover {
    text-decoration: underline;
  }
</style>
